repos:
  - repo: local
    hooks:
      - id: project-lint
        name: Project lint and typecheck
        entry: mise exec -- make lint
        language: system
        pass_filenames: false
        always_run: true
